"use strict";

Object.defineProperty(exports, "__esModule", {
  value: !0
});

var input = require("@arch-ui/input"), React = require("react");

function _interopDefault(e) {
  return e && e.__esModule ? e : {
    default: e
  };
}

var React__default = _interopDefault(React);

const FileFilterView = ({onChange: onChange, filter: filter, field: field, innerRef: innerRef, value: value}) => {
  if (!filter) return null;
  const placeholder = field.getFilterLabel(filter);
  return React__default.default.createElement(input.Input, {
    onChange: event => {
      const value = event.target.value;
      onChange(value.replace(/\D/g, ""));
    },
    ref: innerRef,
    placeholder: placeholder,
    value: value
  });
};

exports.default = FileFilterView;
